<?php include 'template/header.phtml'; ?>

<h1>Staff List</h1>

<!-- New Staff Button -->
<div class="new-staff-container">
    <a href="/staffEdit.php" class="new-staff-button">New Staff</a>
</div>

<!-- Search bar passes the query back to the php -->
<div class="search-container">
    <form method="GET" action="/staffList.php">
        <input type="text" name="search" placeholder="Search staff..." value="<?= htmlspecialchars($searchQuery ?? '') ?>">
        <button type="submit">Search</button>
    </form>
</div>

<!-- Displays the staff list -->
<table class="container">
    <thead>
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>Contacts</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($currentStaffList as $staff): ?>
        <tr>
            <td>
                <form action="/Models/setStaffSession.php" method="post" style="display: inline;">
                    <input type="hidden" name="staffId" value="<?= htmlspecialchars($staff['StaffID']) ?>">
                    <button type="submit" class="btn-link"><?= htmlspecialchars($staff['Name']) ?></button>
                </form>
            </td>

            <td><?= htmlspecialchars($staff['Email']) ?></td>
            <td><?= htmlspecialchars($staff['Phone']) ?></td>
            <td><?= htmlspecialchars($staff['contactCount']) ?></td>

            <td>
                <!-- keeps the values of the staffs id private so the site can use them to handle data but not seen by user. -->
                <form action="/Models/setStaffSession.php" method="post" style="display: inline;">
                    <input type="hidden" name="staffId" value="<?= htmlspecialchars($staff['StaffID']) ?>">
                    <input type="hidden" name="redirect" value="/staffEdit.php">
                    <button type="submit" class="update-button">Update</button>
                </form>
            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<div class="pagination">
    <?php if ($page > 1): ?>
        <a href="?page=1">First</a>
        <a href="?page=<?= $page - 1 ?>">Previous</a>
    <?php endif; ?>

    <?php for ($i = 1; $i <= $totalPages; $i++): ?>
        <a href="?page=<?= $i ?>" class="<?= $i == $page ? 'active' : '' ?>"><?= $i ?></a>
    <?php endfor; ?>

    <?php if ($page < $totalPages): ?>
        <a href="?page=<?= $page + 1 ?>">Next</a>
        <a href="?page=<?= $totalPages ?>">Last</a>
    <?php endif; ?>

    <!-- Page Input -->
    <form action="" method="get" class="page-input-form" style="display: inline;">
        <label for="page-input">Go to page:</label>
        <input type="number" id="page-input" name="page" min="1" max="<?= $totalPages ?>" value="<?= $page ?>" required>
        <button type="submit">Go</button>
    </form>
</div>


<?php include 'template/footer.phtml'; ?>
